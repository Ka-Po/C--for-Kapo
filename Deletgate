using System;

namespace Deletgate
{

    public delegate void de(int a);            //宣告新形態
    public delegate int de2();

    public class MainClass
    {
        //public delegate void de();
        public de a;//方法變數

        public static void Main(string[] args)
        {
            de B = funtion1;            //類似宣告int,string型態
            B(2);

            //Action B2 = funtion1;
            //B2();

            Func<int> func = funtion2;
            Console.WriteLine(func());

            //de C = new de(funtion1);    //類似宣告實例化類別：CL cl = new CL();
            //C();                        //但執行起來都是作為方法去執行

            //MainClass.funtion1();

            //MainClass A = new MainClass();
            //A.a = funtion1;
            //A.a();

            void funtion1(int a = 1)
            {
                Console.WriteLine(a);
            }

            int funtion2()
            {
                return 2;
            }


            Predicate<int> isEven = IsEven;
            Console.WriteLine(isEven(5));

            bool IsEven(int number)
            {
                return number % 2 == 0;
            }

        }



    }


    class CL
    {
    
    }


}
